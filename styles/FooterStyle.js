import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  footerTextStyle: {
    fontSize: 20,
  },
  footerTabStyle: {
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export {styles};
